# Go
/bin/
*.exe
*.test
*.out

# Ambiente
.env
*.local.yaml

# Ferramentas
/tmp/
.DS_Store

# Node — só o toolchain do openapi-typescript. O package-lock.json fica
# versionado (é o que torna o `npm ci` reprodutível); o node_modules não.
/node_modules/

# Documentação interna — vive no Confluence privado, não neste repositório.
# Ver a secção "Documentos" do README. Continuam no disco de quem tem acesso.
#
# ⚠️ `git clean -xdf` APAGA-OS: o `-x` remove precisamente o que está ignorado.
# `git clean -df`, sem o `-x`, não lhes toca. Se acontecer, repõem-se do
# Confluence — não há outra cópia.
/CLAUDE.md
/PLAN.md
/RESUME.md
/docs/
